{
  "name": "vault-print-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test test/weblink.test.ts",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "wrangler": "^4.20.0"
  }
}
